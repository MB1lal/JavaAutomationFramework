webdriver {
  capabilities {
    "moz:firefoxOptions" {
      args = ["--headless", "--disable-gpu", "--no-sandbox", "--safe-mode", "--ignore-certificate-errors", "--incognito"],
      prefs {
        "javascript.options.showInConsole": false
      },
      log {"level": "info"},
    }
  }
}